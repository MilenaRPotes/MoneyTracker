<Window x:Class="MoneyTracker.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MoneyTracker.ViewModels"
        mc:Ignorable="d"
        Title="DashboardWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:DashboardViewModel></vm:DashboardViewModel>
    </Window.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Text="Dashboard Summary" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"></TextBlock>
        
        <!--Summary Section-->
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0">
            <TextBlock Text="{Binding TotalExpenses, StringFormat='Total Expenses: {0:C}'}" FontSize="18" FontWeight="SemiBold" Margin="0,10"/>
            <TextBlock Text="{Binding TotalIncome, StringFormat='Total Income: {0:C}'}" FontSize="18" FontWeight="SemiBold" Margin="0,10"/>
            <TextBlock Text="{Binding Balance, StringFormat='Balance: {0:C}'}" FontSize="20" FontWeight="Bold" Foreground="DarkGreen" Margin="0,20"/>
        </StackPanel>

    </Grid>
</Window>
