<UserControl x:Class="MoneyTracker.Views.UserControls.ExpenseControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MoneyTracker.ViewModels"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:ExpenseViewModel />
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Add New Expense" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20" />

        <!-- Form -->
        <StackPanel Orientation="Vertical" Grid.Row="1" Width="300" HorizontalAlignment="Center" VerticalAlignment="Top">
            <TextBlock Text="Description" />
            <TextBox Text="{Binding Description, Mode=TwoWay}" Margin="0,0,0,10" />

            <TextBlock Text="Amount" />
            <TextBox Text="{Binding Amount, Mode=TwoWay}" Margin="0,0,0,10" />

            <TextBlock Text="Category" />
            <ComboBox ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding Category, Mode=TwoWay}"
                      Margin="0,0,0,10" />

            <TextBlock Text="Date" />
            <DatePicker SelectedDate="{Binding Date, Mode=TwoWay}" Margin="0,0,0,20" />

            <Button Content="Save" Width="100" HorizontalAlignment="Center" Command="{Binding SaveCommand}" />
            <Button Content="Delete" Width="100" HorizontalAlignment="Center" Command="{Binding DeleteExpenseCommand}" Margin="0,10,0,0" />
        </StackPanel>

        <!-- DataGrid -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Your Expenses" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

            <DataGrid ItemsSource="{Binding Expenses}" AutoGenerateColumns="False" CanUserAddRows="False"
                      HeadersVisibility="Column" AlternatingRowBackground="LightGray" Margin="0,0,0,10"
                      VerticalScrollBarVisibility="Auto" IsReadOnly="True" Height="200"
                      SelectedItem="{Binding SelectedExpense, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="100" />
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150" />
                    <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="{Binding TotalExpenses, StringFormat='Total Expenses: {0:C}'}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,0,20,0" />
        </StackPanel>
    </Grid>
</UserControl>
