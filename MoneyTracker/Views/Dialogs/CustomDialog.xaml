<Window x:Class="MoneyTracker.Views.Dialogs.CustomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="222" Width="400"
        Title="{Binding DialogTitle, FallbackValue=Confirm}"
        FontFamily="Segoe UI"
       >

    <Border   BorderBrush="Black"
    BorderThickness="1.5" CornerRadius="12" Padding="20" 
          Background="{StaticResource BackgroundGradient}"
            Effect="{DynamicResource DropShadowEffect}">

        <StackPanel >
            <!--Title-->
            <TextBlock x:Name="DialogTitleBlock" Text="{Binding DialogTitle}" FontSize="18" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,20"  Foreground="{StaticResource TextPrimaryColor}"></TextBlock>

            <!--Message-->
            <TextBlock x:Name="MessageText" TextWrapping="Wrap"  Foreground="#444"  FontSize="14" Margin="0,0,0,20" TextAlignment="Center"></TextBlock>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <!-- Confirm buttons -->
                <StackPanel x:Name="ConfirmationButtons" Orientation="Horizontal">
                    <Button Content="Yes" Width="80" Margin="10,0" Style="{StaticResource DialogButtonStyle}"  Click="Yes_Click"/>
                    <Button Content="No" Width="80" Margin="10,0"  Style="{StaticResource DialogButtonStyle}" Click="No_Click"/>
                </StackPanel>

                <!-- Info button -->
                <StackPanel x:Name="InfoButton" Orientation="Horizontal">
                    <Button Content="OK" Width="80" Margin="10,0" Style="{StaticResource DialogButtonStyle}" Click="Ok_Click"  />
                </StackPanel>
            </StackPanel>
        </StackPanel>

    </Border>
    
   
</Window>
