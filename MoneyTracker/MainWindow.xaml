<Window x:Class="MoneyTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MoneyTracker.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="623" Width="800">

    <Window.DataContext>
        <vm:MainViewModel></vm:MainViewModel>
    </Window.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <!--Title-->
            <RowDefinition Height="Auto"></RowDefinition>
            <!--Form-->
            <RowDefinition Height="Auto"></RowDefinition>
            <!--DataGrid-->
            <RowDefinition Height="*"></RowDefinition>
            
        </Grid.RowDefinitions>
        
       <!-- Title-->
        <TextBlock Grid.Row="0" Text="Add New Expense" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"></TextBlock>
        
        <!--#region STACKPANEL FORM-->
        <StackPanel Orientation="Vertical" Grid.Row="1" Width="300" HorizontalAlignment="Center" VerticalAlignment="Top">
            <TextBlock Text="Description"></TextBlock>
            <TextBox Text="{Binding Description, Mode=TwoWay}" Margin="0,0,0,10"></TextBox>
            
            <TextBlock Text="Amount"></TextBlock>
            <TextBox Text="{Binding Amount, Mode=TwoWay}" Margin="0,0,0,10"></TextBox>   
            
            <TextBlock Text="Category"></TextBlock>
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding Category, Mode=TwoWay}"  Margin="0,0,0,10"></ComboBox>

            <TextBlock Text="Date"></TextBlock>
            <DatePicker SelectedDate="{Binding Date, Mode=TwoWay}" Margin="0,0,0,20"></DatePicker>

            <Button Content="Save" Width="100" HorizontalAlignment="Center"   Command="{Binding SaveCommand}"></Button>

           
        </StackPanel>
        <!--#endregion-->
        
        <!--#region DATAGRID Expenses-->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Your Expenses" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"></TextBlock>

            <DataGrid  ItemsSource="{Binding Expenses}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" AlternatingRowBackground="LightGray"  Margin="0,0,0,10" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description }" Width="*"></DataGridTextColumn>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="100"></DataGridTextColumn>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"></DataGridTextColumn>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Width="120"></DataGridTextColumn>
                </DataGrid.Columns>

            </DataGrid>
            <TextBlock Text="{Binding TotalExpenses, StringFormat='Total Expenses: {0:C}'}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,0,20,0" ></TextBlock>
        </StackPanel>
     
        <!--#endregion-->


    </Grid>
</Window>
